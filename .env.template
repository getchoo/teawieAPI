# shellcheck shell=sh

LISTEN_ADDR="localhost:8000"
GITHUB_TOKEN=""
# This is where the root endpoint redirects to. Default is this repo
# REDIRECT_URL=""

RUST_LOG="teawie_api=debug,server=debug,tower_http=debug,axum=trace,warn"
